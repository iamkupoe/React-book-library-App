import React from 'react';

const Header = () => {
    return(
        <header>
         <i class="fas fa-book-alt"></i>
         <h1>Book Library App</h1>
        </header>
    );
}

export default Header;